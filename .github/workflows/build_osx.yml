name: Build libFAUDES on OsX

on:
  workflow_dispatch:
      
jobs:

  configure:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Install Extra Libs
      run: |
          sudo apt-get install doxygen swig
          sudo apt-get install libreadline-dev
    - name: Figure Version
      run: |
        . ./VERSION
        echo "VMAJOR=$FAUDES_VERSION_MAJOR" >> "$GITHUB_ENV"
        echo "VMINOR=$FAUDES_VERSION_MINOR" >> "$GITHUB_ENV"
    - name: Configure libFAUDES v${{ env.VMAJOR }}.${{ env.VMINOR }}
      run: |
        make -j configure
        make clean
        tar -czf faudes.tgz ./*
    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: faudes_configured_src.tgz
        path: ./faudes.tgz
        if-no-files-found: error
        compression-level: 0

  build:  

    runs-on: macos-latest
    needs: [configure]
    
    steps:	
    - name: Download Artifact
      uses: actions/download-artifact@v4
      with:
        name: faudes_configured_src.tgz
        path: ./	
    - name: Extract Archive
      run: |
        ls
        tar -xzf faudes.tgz
    - name: Install Tools
      run: |
        brew update
        brew install make
        brew install swig
        brew install perl
        brew install doxygen
    - name: Setup Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.13'
    - name: Test Python
      run: python --version 
    - name: Build Default Targets
      run: |
        gmake -j1
    - name: Build Tutorials
      run: gmake -j1 tutorial
    - name: Run Test Cases
      run: gmake -j1 test
    - name: Build Package
      run: |
        make PYTHON=python package
        ls libfaudes_*.*
    - uses: actions/upload-artifact@v4
      with:
        name: libfaudes_${{ env.VMAJOR }}_${{ env.VMINOR }}.tar.gz
        path: ./libfaudes_*.tar.gz
        compression-level: 0
        if-no-files-found: error
